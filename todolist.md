# TRPG 游戏引擎 - 待办事项清单

> 最后更新：2026-06-01
> 本文件整合 `.trae/documents/` 中的计划文档和 `newdesign.txt` 中的 TODO

---

## 🔔 AI 助手维护提醒（必读）

> **每次打开此文件时，请先阅读以下规范，确保维护一致性：**

### 1. 状态标记规范
- 使用表格中的标准标记（⏳🚧🧪🔍👤🧑‍🤝‍🧑✅）
- 不要自创标记，保持统一

### 2. 任务增删原则
- **新增任务**：添加到对应优先级区块，标记为 ⏳
- **完成任务**：更新标记，填写完成时间
- **删除任务**：仅在确认功能废弃后删除，不要仅因完成而删除

### 3. 测试阶段推进
- 每完成一个功能，必须更新其测试阶段标记
- 单元测试通过（🧪）后才能推进到人工测试（👤）

### 4. 文件关联
- 每个任务应关联相关文件路径
- 计划文档放在 `.trae/documents/`，完成后归档

### 5. 更新日志
- 每次修改后更新 "最后更新" 日期
- 重大变更在文件末尾添加变更记录

---

## 📋 测试阶段标记规范（参考业界标准）

| 标记 | 阶段 | 定义 | 通过标准 |
|------|------|------|----------|
| ⏳ | **待开始** | 需求明确，尚未进入开发 | - |
| 🚧 | **开发中** | 代码实现阶段，可能有单元测试 | 语法检查通过 |
| 🧪 | **单元测试通过** | 自动化测试覆盖核心逻辑 | `python -m unittest` 全绿 |
| 🔍 | **集成测试通过** | 多模块联调，接口契约验证 | 端到端流程跑通 |
| 👤 | **人工测试通过** | 实机验证，用户体验确认 | 开发者自测无阻塞 bug |
| 🧑‍🤝‍🧑 | **用户测试通过** | 真实用户场景验证 | 用户反馈无核心功能缺陷 |
| ✅ | **已发布** | 功能上线，进入维护期 | 生产环境稳定运行 |

**测试轮次建议**：
1. **第1轮 - 单元测试**：开发时同步编写，确保函数级正确性
2. **第2轮 - 集成测试**：模块联调，验证数据流和接口
3. **第3轮 - 系统测试**：完整用户流程，边界条件覆盖
4. **第4轮 - 验收测试**：用户视角验证，体验优化

---

## 🔴 高优先级（当前开发重点）

### 复杂对抗环境正式实现
- **来源**: `newdesign.txt` Phase 1
- **状态**: 🧪 单元测试通过
- **测试覆盖**: `test_complex_encounter.py` (43+ 测试用例)
- **描述**: 第一版仅覆盖"玩家复合动作"，即玩家在同一轮中显式声明的连续动作、多目标动作、先后物品流转与条件依赖；暂不引入 NPC 主动回合、AP、速度、完整群战 AI
- **目标**: 处理"夺剑后反击""先用道具再攻击""同时影响两名目标"等复杂玩家输入
- **设计接口**: `ComplexEncounterResolver.resolve(action_sequence, major_graph, pc_name, world_anchor_text, active_scene=None) -> dict`
- **相关文件**: `rules/complex_encounter.py`, `turn_engine.py`
- **待完善**: 
  - [ ] 实机测试验证（玩家实际游戏体验）
  - [ ] NPC 主动回合扩展（未来版本）

### 物品名称 Canonical 归一
- **来源**: `.trae/documents/item-canonical-normalization-plan.md`
- **状态**: 🧪 单元测试通过（兼容版已在沙盒落地，待进一步场景联调）
- **描述**: 解决"墙上的火把"vs"火把"vs"燃烧的火把"被当作不同物品的问题，同时避免真的存在两个火把时被误合并
- **核心机制**: 直接引入全局物品实例池 `item_instances`，用 `canonical_name` 表示物品类型，用 `instance_id` 表示具体实体；保留旧 `6_inventory` / `7_held_items` 作为兼容索引
- **设计文档**: `docs/superpowers/specs/2026-06-01-global-item-instances-design.md`
- **涉及文件（沙盒优先）**: `sandbox_item_instances.py`（新增）, `sandbox_intent_parser.py`, `sandbox_rules/complex_encounter.py`, `sandbox_rules/adjudication_utils.py`, `sandbox_integration.py`, `sandbox_app.py`, `sync_sandbox_to_main.py`
- **兼容版实施目标**:
  - [ ] 新增 `major_graph["item_instances"]` 全局实例池
  - [ ] 旧 `6_inventory` / `7_held_items` 自动迁移到实例池，并写回 `instance_id`
  - [ ] 新物品创建时写入实例池，同时同步旧字段兼容索引
  - [ ] 建立 `resolve_item_reference()`，支持同名多实例消歧
  - [ ] 覆盖火把多实例、手持优先、墙上优先、再拿一支、点燃不新增等测试
- **兼容版 → 完全版过渡施工思路**:
  1. **阶段1：兼容版落地** — 新增实例池，旧字段继续可读；新旧结构双写，保证现有裁决/UI不崩。
  2. **阶段2：读路径切换** — 裁决系统、日志系统优先读 `item_instances`，旧字段作为 fallback。
  3. **阶段3：写路径切换** — 门铃同步、复合动作、奖励/消耗只写实例池，旧字段由 `sync_legacy_item_index()` 自动生成。
  4. **阶段4：UI与调试面板升级** — 普通 UI 聚合展示（如 `火把 ×2`），调试 UI 展示每个 `instance_id`、位置、状态、持有者。
  5. **阶段5：完全版** — 引入 `6_inventory_refs` / `7_held_item_refs`，旧 `6_inventory` / `7_held_items` 降级为只读兼容视图，最终删除旧写入路径但保留旧存档迁移。

### NPC 证据检索与身份解析 Gate
- **来源**: `docs/superpowers/specs/2026-06-01-npc-evidence-identity-resolution-design.md`
- **状态**: 🧪 单元测试通过（第一阶段已在沙盒落地，待专项实机联调）
- **描述**: 在 NPC 初始化前执行证据检索与身份解析 gate，防止 `神秘人`、`黑衣人`、`师父`、物品/组织等表面称呼被粗暴初始化为长期 NPC
- **核心机制**: `sandbox_npc_lifecycle.py` 提供 `identity` 子结构、`pending_references`、scoped alias、证据句段检索与初始化 gate
- **已覆盖测试**:
  - [x] `神秘人` 命中已有 `伏地魔` alias 时不新建 NPC
  - [x] 无证据 `神秘人` 进入 pending，不初始化
  - [x] `萧峰` 解析到已有 `乔峰`，不改 graph key
  - [x] `黑衣人` 揭示为 `慕容复` 时只生成 `scene_bound` alias
  - [x] 后续“另一个黑衣人”不自动解析到慕容复
  - [x] `墙上的火把` 等非角色 annotation 被拒绝初始化
  - [x] `山贼` 在 annotation 授权时允许初始化
- **后续施工思路**:
  1. **阶段2：LLM 结构化身份解析** — 当前确定性解析器只是第一阶段测试桩，后续应替换为“Python 召回证据 → LLM 结构化判断 → Python 校验落盘”。
  2. **阶段3：pending reference 回填** — 后续剧情揭示身份时，将此前挂起的 `pending_references` 回填为 alias 或合并到已有 NPC。
  3. **阶段4：NPC 更新 patch 化** — 门铃同步不直接改 NPC，而是输出 patch，经 Python gate 审核后应用。
  4. **阶段5：能力新增 gate** — 只有“领悟/学会/觉醒/突破/获得传承”等强证据才允许新增能力，防止“反击/挥刀/闪避”污染能力树。
  5. **阶段6：完整 NPC 实例化** — 未来再考虑 `npc_instances`，解决同名多人、秘密身份、事实身份与玩家认知分离。

### 风险结果边界与动作标题修复
- **来源**: `.trae/documents/risk-result-boundary-and-action-title-plan.md`
- **状态**: ⏳ 待开始
- **问题 1**: `world_state_change` 在机制裁判前预写成功结果（如"山贼被逼退"）
- **问题 2**: 顶层 `intended_action` 输出机器标签（如 `use_tool`）而非自然语言
- **涉及文件**: `intent_engine.py`, `turn_engine.py`

### 动作序列重构
- **来源**: `newdesign.txt`
- **状态**: 🧪 单元测试通过
- **测试覆盖**: `test_complex_encounter.py` 复合动作测试
- **描述**: 将 `obtained_items` / `lost_items` 改为有序动作列表，支持"先获得后丢失"的复杂交互
- **备注**: 旧字段保留作为兼容层

### 细招→主招式框架
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 招式支持 `parent` 字段指向主招式，读取细招时自动返回主招式的熟练度
- **涉及**: 数据结构扩展 + NPC 初始化提示词更新 + 检定系统适配

### 技能-物品依赖校验
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 剑招需要手持剑类武器，需扩展技能数据结构 + 区分装备/携带

---

## 🟡 中优先级（功能完善）

### 检定日志展示手持物变动
- **来源**: `.trae/documents/held-items-log-display-plan.md`
- **状态**: ⏳ 待开始
- **描述**: 在机制检定日志（黑匣子）中展示 `state_patch` 的物品变动信息
- **目标**: 让用户直观看到火把等物品进入了手持物层还是背包
- **涉及文件**: `turn_engine.py`, `app.py`

### 无效信息防污染
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 被校验否决的回合在 `active_scene` 中打标记，防止幻觉叙事传播

### 门铃提炼能力获得判定
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **问题**: 当前"反击"被误判为新能力
- **需求**: 只有叙事中包含"领悟/学会/觉醒"等关键词才算新能力

### 招式语义匹配接入检定链路
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: `ability_matcher.py` 已存在，需正式接入能力识别与检定链路
- **目标**: 解决"偷袭拳"vs"偷袭一拳"等招式名变体问题

### 私人笔记持久化字段隔离
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 明确 schema 边界，确保摘要、门铃同步不会把玩家私人笔记误送入 LLM

### 开发者模式调试面板整理
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 将幕间推进、检定日志、资产变更日志统一为可复制、可折叠、可导出的调试面板

---

## 🟢 低优先级 / 架构重构

### NPC AI 回合系统
- **来源**: `newdesign.txt` Phase 2
- **状态**: ⏳ 待开始
- **描述**: LLM 控制 NPC 主动行动 + 反向意图解析 + 玩家被动应对检定
- **依赖**: 需与连招/克制系统同步设计

### 连招系统（AP/速度）
- **来源**: `newdesign.txt` Phase 2
- **状态**: ⏳ 待开始
- **描述**: 招式耗时（AP）+ 速度属性 = AP 上限 + 顺序截取 + 跨回合延后执行 + 连招减免
- **依赖**: 需新增 AP 计算层

### 招式克制/弱点系统
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 每个招式有弱点（被什么克制）和对策（克制什么），影响检定乘区

### 数值系统模块化
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 抽象 `NumericEngine` 接口，支持 DND/COC/FATE 等规则包接入

### 整体数值平衡重构
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 实战数据驱动调参，各数值节点平衡关系重新设计

### 背景设定解析
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: "前世留学"类表述自动添加能力

### 物品数量属性
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 容器/内容物、耐久度、部分消耗、场景共享

### 撤回快照持久化
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 刷新页面不丢失撤回栈（后悔药）

### core_engine.py 职责拆分
- **来源**: `newdesign.txt`
- **状态**: ⏳ 待开始
- **描述**: 长期将登录、世界观威力表、叙事生成、转场摘要、世界线操作拆入更清晰的正式模块
- **原则**: 沙盒侧不得直接依赖旧正式管线

---

## ✅ 已完成 / 已阶段性完成

| 任务 | 完成时间 | 测试阶段 | 备注 |
|------|---------|----------|------|
| Token优化：移除二次判断逻辑 | 2026-06-01 | 🧪 单元测试通过 | 每次回合减少1次FLASH调用（~300-500 tokens） |
| Token优化：客户端单例缓存 | 2026-06-01 | 🧪 单元测试通过 | `get_user_client()` 缓存OpenAI实例，避免重复创建 |
| Token优化：Prompt模板静态化 | 2026-06-01 | 🧪 单元测试通过 | 预扫描/意图解析/裁决prompt提升为模块级常量，提高API缓存命中率 |
| 二次判断移除回归测试 | 2026-06-01 | 🧪 单元测试通过 | `test_secondary_match_removal.py` - 验证意图解析独立识别NPC能力 |
| CLI测试运行器 | 2026-06-01 | ✅ 已完成 | `cli_test_runner.py` - 绕过UI直接测试核心引擎，自动读取.env API密钥 |
| 上下文资产加权系统（火把数值加成） | 2026-06-01 | 🧪 单元测试通过 | `contextual_multiplier` 实现 |
| 后悔药重新登录后丢失 | 2026-06-01 | 👤 人工测试通过 | 存入存档 JSON |
| consumed_items 失败时语义区分 | 2026-06-01 | 🧪 单元测试通过 | 方案 E 实现 |
| temporary_items 6维度约束校验 | 2026-06-01 | 🧪 单元测试通过 | `validate_item_constraints` |
| action_constraints 动作级约束校验 | 2026-06-01 | 🧪 单元测试通过 | `validate_action_constraints` |
| 令狐冲错误初始化修复 | 2026-06-01 | 👤 人工测试通过 | prompt + 叙事文本校验 |
| 叙事生成无视约束失败修复 | 2026-06-01 | 👤 人工测试通过 | 强约束指令注入 |
| 所有子动作约束失败直接拒绝 | 2026-06-01 | 👤 人工测试通过 | 不进入叙事生成 |
| 重命名世界后后悔药清零修复 | 2026-06-01 | 👤 人工测试通过 | 迁移 undo_stack |
| AI建议按钮+绿色气泡建议 | 2026-06-01 | 👤 人工测试通过 | 含复制功能 |
| 正式入口迁移 | 2026-05-30 | ✅ 已发布 | `app.py` / `turn_engine.py` |
| 规则系统模块化 V1 | 2026-05-30 | ✅ 已发布 | `rules/` 工厂模式 |
| 模糊匹配世界观战力表索引 | 2026-05-30 | ✅ 已发布 | `ability_matcher.py` |
| 物品威力体系侧栏展示 V1 | 2026-05-30 | ✅ 已发布 | 稀有度量表 |
| 幕间推进器 V1 | 2026-05-30 | ✅ 已发布 | `extract_memory_summary` |
| UI 美化 V1 | 2026-05-30 | ✅ 已发布 | 设置弹窗、后悔药等 |

---

## 📋 待验证 / 待测试

- [ ] 沙盒系统实机测试 (`sandbox_app.py`)
- [ ] LLM 语义意图解析准确性验证
- [ ] 多乘区检定公式正确性验证
- [ ] 门铃机制状态同步验证
- [ ] 旧系统 STATUS_UPDATE 兼容性验证
- [ ] 转场系统兼容性验证

---

## 🗂️ 相关文档

| 文档 | 路径 | 内容 |
|------|------|------|
| 核心设计文档 | `newdesign.txt` | 设计哲学、路线图、已完成清单 |
| 物品归一计划 | `.trae/documents/item-canonical-normalization-plan.md` | 火把别名问题解决方案 |
| 风险边界计划 | `.trae/documents/risk-result-boundary-and-action-title-plan.md` | world_state_change 修复 |
| 手持物日志计划 | `.trae/documents/held-items-log-display-plan.md` | 黑匣子物品变动展示 |
| 令狐冲修复记录 | `.trae/documents/fix-linghuchong-spurious-init.md` | 问题分析与修复方案 |

---

*本清单由 AI 助手整理生成，建议定期更新状态。*
