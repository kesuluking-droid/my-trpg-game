# TRPG 游戏引擎 - 核心设计原则

> 本文档整合 `newdesign.txt` 中的设计哲学，作为开发和维护的根本准则
> 最后更新：2026-06-01

---

## 一、核心设计哲学

### 1. 「LLM 造世，Python 书记」

**根本原则**：大语言模型是世界的造物主，Python 是它的书记员和算盘。

| 角色 | 职责 | 边界 |
|------|------|------|
| **LLM** | 世界观、剧情走向、NPC 反应、随机事件 | 拥有最大自由度，即兴创造戏剧性 |
| **Python** | 记忆管理、状态追踪、数值计算 | 补足 AI 的功能弱点，不限制 AI 创作 |

**因果不可倒置**：
- ✅ 「LLM 创造剧情 → Python 同步状态」
- ❌ 「Python 判定结果 → LLM 只是翻译成文字」

---

### 2. 「Python 是 AI 的外置大脑，而非枷锁」

Python 函数的本质不是限制 AI，而是**补足 AI 的功能弱点**。

**AI 的已知弱点**：
- 数值计算
- 逻辑一致性
- 长期记忆
- 状态追踪
- 时间线管理

**外置大脑机制**：
- 在 AI 明确不擅长的领域（如数值对抗），Python 函数拥有"绝对权力"来裁决
- 这不是剥夺 AI 的自由，而是让 AI 获得可靠的外置计算模块
- AI 可以识别自己的弱点，涌现出新的调用工具来补丁

**裁决与参考的边界**：
| 领域 | Python 权力 | AI 角色 |
|------|------------|---------|
| 数值对抗 | **绝对裁决权** | 接收结果，编织叙事 |
| 叙事创作 | 仅提供状态快照 | **绝对自由** |

---

### 3. 「反黑白名单原则」

**尽可能避免硬编码黑名单、白名单、关键词表**来处理开放世界语义问题。

**问题**：
- TRPG 世界是开放的，任何物体、角色、地点、事件都可能进入叙事
- 靠列举"墙、火把、桌子、门"这类词表修补，短期有效，长期导致规则膨胀

**正确方向**：
- 用**结构化语义分类**替代词表
- 让 LLM 或语义层判断实体在当前语境中的**本体类型**
- Python 只检查结构化类型与权限边界
- 对不确定实体保持**保守策略**，不轻易写入核心 NPC 图谱

**示例**：
```json
{
  "name": "墙上的火把",
  "entity_type": "object",
  "role_in_action": "temporary_tool",
  "persistence": "ephemeral",
  "should_initialize_npc": false
}
```

---

### 4. 「沙盒管线绝对隔离」

**沙盒管线是安全开发环境，必须与旧正式管线保持隔离**。

**原则**：
- 沙盒需要使用旧管线函数时，**不应直接调用**，而应将该函数**复制到沙盒侧**并进行 1:1 实现与验证
- 沙盒修改不得污染或依赖旧流程
- 当沙盒功能验证准备进入正式版本时，应将已验证代码**实质迁移**到正式命名文件

**文件对应关系**：
| 主版本 | 沙盒版本 |
|--------|----------|
| `turn_engine.py` | `sandbox_turn_engine.py` |
| `core_engine.py` | `sandbox_core_engine.py` |
| `intent_engine.py` | `sandbox_intent_engine.py` |
| `rules/` | `sandbox_rules/` |

---

### 5. 「世界不以玩家为绝对中心」

世界是一个独立运行的有机体。NPC 有自己的利益诉求、行动逻辑和人际网络。

**信息不对称**：
- 玩家只能通过自身视角（所见、所闻、所触）获取信息
- NPC 的密谋、远方的战争、暗中的交易，只有在玩家主动触及或偶然撞见时才会浮出水面

**事件因果链**：
- 被事件影响的 NPC 会携带事件后果继续与世界交互
- 形成蝴蝶效应般的因果链

**世界聚焦因子**（可调）：
- 高聚焦 = 世界围绕玩家运转（传统 TRPG 体验）
- 低聚焦 = 玩家只是世界中的一个普通人

---

## 二、开发工作流

### 测试阶段标记

| 标记 | 阶段 | 定义 | 通过标准 |
|------|------|------|----------|
| ⏳ | **待开始** | 需求明确，尚未进入开发 | - |
| 🚧 | **开发中** | 代码实现阶段 | 语法检查通过 |
| 🧪 | **单元测试通过** | 自动化测试覆盖核心逻辑 | `python -m unittest` 全绿 |
| 🔍 | **集成测试通过** | 多模块联调，接口契约验证 | 端到端流程跑通 |
| 👤 | **人工测试通过** | 实机验证，用户体验确认 | 开发者自测无阻塞 bug |
| 🧑‍🤝‍🧑 | **用户测试通过** | 真实用户场景验证 | 用户反馈无核心功能缺陷 |
| ✅ | **已发布** | 功能上线，进入维护期 | 生产环境稳定运行 |

### 开发流程

1. **在沙盒版本开发**（`sandbox_*.py`）
2. **编写单元测试**（`test_*.py`）→ 达到 🧪
3. **人工测试验证** → 达到 👤
4. **将验证后的代码实质迁移到主版本**
5. **禁止简单让主版本跳转到沙盒版本**

---

## 三、代码规范

### 文件头模板

```python
"""
文件名.py — 功能描述

================================================================================
🔔 AI 助手维护提醒（每次修改前必读）
================================================================================

【如果是沙盒版本】
- 本文件是 X.py 的 1:1 沙盒镜像副本
- 所有导入必须指向 sandbox_ 前缀版本
- 禁止直接调用主版本

【修改步骤】
1. 如需修改功能，先在此沙盒版本测试验证
2. 验证通过后，将修改实质迁移到主版本
3. 不要简单让主版本跳转到沙盒版本

================================================================================

【设计哲学】
（简述本模块的设计原则）

【核心流程】
（简述主要功能流程）
"""
```

### 测试规范

- **命名**：`test_{{given}}_when_{{condition}}_should_{{expected}}`
- **结构**：AAA 模式（Arrange → Act → Assert）
- **组织**：按功能领域分组
- **固件**：使用基类提供通用固件

---

## 四、架构分层

```
┌─────────────────────────────────────────┐
│  UI 层 (app.py / sandbox_app.py)        │
│  - Streamlit 界面                        │
├─────────────────────────────────────────┤
│  回合控制层 (turn_engine.py)            │
│  - 意图解析 → 校验 → 检定 → 叙事        │
├─────────────────────────────────────────┤
│  规则引擎层 (rules/)                    │
│  - 多乘区检定、复合对抗、社交检定等      │
├─────────────────────────────────────────┤
│  核心服务层 (core_engine.py)            │
│  - LLM 调用、NPC 初始化、存档管理        │
├─────────────────────────────────────────┤
│  数据层 (session_state / JSON)          │
│  - 图谱、存档、配置                      │
└─────────────────────────────────────────┘
```

---

## 五、关键术语

| 术语 | 定义 |
|------|------|
| **门铃机制** | LLM 自由创作后，Python 事后提炼状态变更 |
| **六维数据** | NPC 的完整能力结构（关系、状态、能力、经验、特质、背包） |
| **复合对抗** | 玩家在同一轮中的连续动作、多目标、物品流转 |
| **临时物品** | `persistence=ephemeral`，回合结束后不进入背包 |
| **约束校验** | 6 维度物品约束 + 4 维度动作约束 |
| **上帝模式** | 跳过所有约束校验，最大化满足玩家想象力 |

---

*本文档是项目的根本准则，所有开发决策应以此为准。*
